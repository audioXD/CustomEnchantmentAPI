configure( subprojects - project (':modules:API') ) {
    dependencies {
        compile(project (':modules:API')) {
            transitive = false
        }
        compile fileTree(dir: 'libs', include: '*.jar')
    }
}